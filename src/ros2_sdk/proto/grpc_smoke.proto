syntax = "proto3";

package ros2_sdk.smoke;

service SmokeService {
  rpc Echo(EchoRequest) returns (EchoResponse);
}

message EchoRequest {
  string payload = 1;
}

message EchoResponse {
  string payload = 1;
}
