# RosBridge Pro

RosBridge Pro 将机器人能力表达为稳定的领域操作，并把 ROS 2 和具体机器人接口隐藏在运行时
内部。

## 领域语言

**Conan CI 访问凭据配置流程**：维护者为只读 Conan CI 身份准备凭据、访问策略并完成可信验证的
一次性流程；它包含凭据轮换和紧急撤销语义。
_避免_：Conan CI 访问配置编排、Conan CI SSH 向导、普通 CI 构建流程

**配送请求（Delivery Request）**：
配送客户端提出的任务创建意图，包含一个取货地点和一个送达地点。
_避免_：配送任务

**配送请求 ID（Delivery Request ID）**：
由配送客户端为一次配送请求选择的标识；安全重试同一意图时复用该标识。
_避免_：任务 ID

**配送任务（Delivery Task）**：
已被接受的业务请求，表示一台机器人将物品从取货地点运送到送达地点。
_避免_：导航任务、导航目标

**活动配送任务（Active Delivery Task）**：
尚未进入终态并独占机器人的配送任务，包括等待人工确认或取消结果的阶段。
_避免_：排队任务

**导航目标（Navigation Goal）**：
机器人执行配送任务时，前往一个目标地点的单段移动请求。
_避免_：配送任务

**配送客户端（Delivery Client）**：
不直接使用 ROS 2、负责创建和控制配送任务的外部应用。
_避免_：ROS 客户端

**配送地点（Delivery Location）**：
用于取货或送达物品的稳定业务地点名称。
_避免_：Pose、ROS waypoint

**取货确认（Pickup Confirmation）**：
机器人到达取货地点后，确认物品已经装载的业务动作。
_避免_：导航结果

**送达确认（Drop-off Confirmation）**：
机器人到达送达地点后，确认物品已经卸载的业务动作。
_避免_：导航结果

**请求拒绝（Request Rejection）**：
系统在创建配送任务之前拒绝配送请求。
_避免_：任务失败

**任务失败（Task Failure）**：
配送任务被接受后，以未成功完成的终态结束。
_避免_：请求拒绝

**配送就绪（Delivery Readiness）**：
运行时能够接受并开始配送任务的状态，与服务进程是否存活相互独立。
_避免_：进程存活
